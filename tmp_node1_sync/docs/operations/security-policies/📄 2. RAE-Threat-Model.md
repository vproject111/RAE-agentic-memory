📄 2. RAE-Threat-Model.md
Threat Model — STRIDE for RAE
1. Spoofing

ataki na API → mitigacja: API keys + mTLS

podszywanie się pod integracje → signature verification

2. Tampering

manipulacja pamięcią → hashing + audit

modyfikacja modeli → version pinning + signatures

3. Repudiation

zaprzeczenie operacjom → immutable audit logs

4. Information Disclosure

przypadkowy eksport pamięci → HIL + isolation

wyciek tokenów → vault + rotation

5. Denial of Service

ataki kosztowe → token-guard + throttling

przeciążenie Qdrant → rate limits

6. Elevation of Privilege

manipulacja reflective memory → trusted execution paths

injection do LLM → filters + role boundaries